import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginBottom: 12,
  },
  label: {
    fontSize: 14,
    marginBottom: 6,
    color: '#555',
  },
  errorMessage: {
    color: 'red',
    fontStyle: 'italic',
    fontSize: 12,
  },
});
